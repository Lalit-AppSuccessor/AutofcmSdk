name: autofcm_sdk
description: >
  AutoFCM SDK for in-app notification & FCM automation
version: 1.0.0
environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=3.7.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0
  shared_preferences: ^2.2.2
  firebase_messaging: ">=14.0.0 <17.0.0"

dev_dependencies:
  flutter_lints: ^3.0.0
